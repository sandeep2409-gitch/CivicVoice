Act as a Senior Full-Stack Engineer. I want to build a complete web application called Civic Voice, an AI-powered civic issue reporting platform. Generate the complete, production-ready codebase for this application. We will use Next.js 15 (App Router), TypeScript, Tailwind CSS, Firebase (Auth + Firestore), and the @google/genai SDK.

Please provide all file contents in complete, copy-pasteable code blocks without placeholder comments. Do not leave out imports. Follow these exact, detailed file-by-file instructions to build the app:

1. `package.json`
- Include the standard Next.js 15, React 19, and Tailwind CSS v4 dependencies.
- Add these specific packages to dependencies: `firebase`, `@google/genai`, `lucide-react`, and `recharts`.

2. `.env.local`
- Create a template for the environment variables.
- Include: NEXT_PUBLIC_FIREBASE_API_KEY, NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN, NEXT_PUBLIC_FIREBASE_PROJECT_ID, NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET, NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID, NEXT_PUBLIC_FIREBASE_APP_ID.
- Include: GEMINI_API_KEY.

3. `app/globals.css`
- Import Tailwind v4 (`@import "tailwindcss";`).
- Define the following CSS variables in `:root`:
  --background: #f8fafc;
  --foreground: #0f172a;
  --accent-blue: #2563eb;
  --card-bg: #ffffff;
  --card-border: #e2e8f0;
  --secondary-bg: #f1f5f9;
- Define standard styles for `.card` (shadows and hover effects), `.btn-primary` (using accent-blue), `.btn-secondary`, and input fields (`.input-field`).
- Define badge colors: `.badge-high` (red-100 bg, red-600 text), `.badge-medium` (yellow-100 bg, yellow-600 text), `.badge-low` and `.badge-resolved` (green-100 bg, green-600 text), `.badge-pending` (gray-100 bg, gray-600 text).

4. `lib/firebase.ts`
- Initialize the Firebase App using the environment variables.
- Export `app`, `auth` (getAuth), and `db` (getFirestore).

5. `lib/gemini.ts`
- Initialize and export the Google Gen AI client (`new GoogleGenAI({ apiKey: process.env.GEMINI_API_KEY })`).

6. `services/reportService.ts`
- Export a function `saveReport(report: any)` that takes report data.
- Use `addDoc` to save it to a `reports` collection in Firestore.
- Merge the incoming data with default values: `status: "Pending"`, `confirmations: 0`, and `createdAt: serverTimestamp()`.

7. `app/context/AuthContext.tsx`
- Create a React Context provider that listens to Firebase's `onAuthStateChanged`.
- Provide the `user` object and a `loading` boolean to the rest of the application.

8. `app/components/Navbar.tsx`
- A responsive top navigation bar.
- Include links to: Home, Report Issue, Community Feed, and Dashboard.
- Show Login/Logout buttons based on the AuthContext state.

9. `app/components/ProtectedRoute.tsx`
- A wrapper component that takes `children`.
- It should check AuthContext; if `loading` is true, show a spinner. If `user` is null, redirect to `/login` or `/admin/login`. Otherwise, render `children`.

10. `app/api/analyze/route.ts` (POST)
- Receive `{ description, location }` from the request JSON.
- Call the Gemini API using model `gemini-3.1-flash-lite`.
- Pass a prompt asking the AI to act as a civic issue analysis agent, analyze the description, and return a strict JSON response containing: `category`, `severity` (High/Medium/Low), `summary`, and `suggestedAction`.
- Clean the response text to remove markdown codeblocks and parse the JSON before returning it to the client.

11. `app/api/vision/route.ts` (POST)
- Receive `{ imageBase64, location }` from the request JSON.
- Call the Gemini API using model `gemini-2.5-flash`.
- Pass the inline image data and a text prompt asking it to analyze the civic issue image and return strict JSON with: `category`, `severity`, `summary`, and `suggestedAction`.
- Clean, parse, and return the JSON.

12. `app/page.tsx`
- The landing page with a dark-themed hero section (`bg-slate-900`) and a feature grid on a white background below.
- Add primary call-to-action buttons linking to `/report` and `/feed`.

13. `app/report/page.tsx`
- A client-side form for submitting issues.
- Include an input for Location.
- Allow users to either upload an Image or type a text Description.
- Include a button to "Analyze with AI". When clicked, call the respective API endpoint (`/api/vision` for images, `/api/analyze` for text).
- Display the AI's returned `category`, `severity`, `summary`, and `suggestedAction` to the user for review.
- Add a "Submit Report" button that calls `saveReport` from `reportService.ts` to save the final payload to Firestore, then redirect to `/feed`.

14. `app/feed/page.tsx`
- A public community feed.
- Fetch all documents from the `reports` Firestore collection, ordered by `createdAt` descending.
- Display them as cards using the styling defined in `globals.css` (use `.badge` classes for severity and status).

15. `app/dashboard/page.tsx`
- A statistics dashboard.
- Fetch all reports from Firestore.
- Process the data into counts (e.g., reports by category, reports by status).
- Render at least one chart (e.g., a BarChart or PieChart) using `recharts` to visualize the data.

16. `app/admin/login/page.tsx`
- A simple admin login form (Email and Password).
- Use Firebase `signInWithEmailAndPassword` to authenticate. On success, redirect to `/admin`.

17. `app/admin/page.tsx`
- Wrap this page's content in `<ProtectedRoute>`.
- Fetch all reports from Firestore.
- Display them in a data table.
- Include a dropdown or button for each row allowing the admin to update the status of the report (e.g., from "Pending" to "Resolved"). Update the document in Firestore when changed.

Please generate the full, un-truncated code for every file specified above.

---

### Reference Code (Existing Application)

Below is the exact code for the core files of the application for your reference. 

**`app/globals.css`**
```css
@import "tailwindcss";

:root {
  --background: #f8fafc; /* slate-50 */
  --foreground: #0f172a; /* slate-900 */
  --accent-blue: #2563eb; /* blue-600 */
  --card-bg: #ffffff;
  --card-border: #e2e8f0; /* slate-200 */
  --secondary-bg: #f1f5f9; /* slate-100 */
}

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: var(--font-geist-sans);
  --font-mono: var(--font-geist-mono);
}

/* ── Base ─────────────────────────────────── */
body {
  background: var(--background);
  color: var(--foreground);
  font-family: var(--font-sans), system-ui, -apple-system, sans-serif;
  -webkit-font-smoothing: antialiased;
}

/* ── Animations ───────────────────────────── */
@keyframes fadeIn {
  from { opacity: 0; }
  to   { opacity: 1; }
}
@keyframes slideUp {
  from { opacity: 0; transform: translateY(12px); }
  to   { opacity: 1; transform: translateY(0); }
}

.animate-fade-in {
  animation: fadeIn 0.4s ease-out both;
}
.animate-slide-up {
  animation: slideUp 0.5s ease-out both;
}

/* Stagger children */
.stagger > *:nth-child(1) { animation-delay: 0ms; }
.stagger > *:nth-child(2) { animation-delay: 50ms; }
.stagger > *:nth-child(3) { animation-delay: 100ms; }
.stagger > *:nth-child(4) { animation-delay: 150ms; }
.stagger > *:nth-child(5) { animation-delay: 200ms; }
.stagger > *:nth-child(6) { animation-delay: 250ms; }

/* ── Card ───────────────────────────── */
.card {
  background: var(--card-bg);
  border: 1px solid var(--card-border);
  border-radius: 12px;
  box-shadow: 0 1px 2px 0 rgb(0 0 0 / 0.05);
  transition: all 0.2s ease-in-out;
}
.card:hover {
  box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
}

/* ── Button base ──────────────────────────── */
.btn-primary {
  background: var(--accent-blue);
  color: white;
  padding: 0.75rem 1.5rem;
  border-radius: 0.375rem; /* rounded-md */
  font-weight: 600;
  font-size: 0.875rem;
  transition: all 0.2s ease;
  border: none;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
}
.btn-primary:hover {
  background: #1d4ed8; /* blue-700 */
}
.btn-primary:active {
  transform: translateY(0);
}
.btn-primary:disabled {
  opacity: 0.6;
  cursor: not-allowed;
  transform: none;
}

.btn-secondary {
  background: #f3f4f6; /* gray-100 */
  color: #1e293b; /* slate-800 */
  padding: 0.75rem 1.5rem;
  border-radius: 0.375rem; /* rounded-md */
  font-weight: 600;
  font-size: 0.875rem;
  border: none;
  cursor: pointer;
  transition: all 0.2s ease;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
}
.btn-secondary:hover {
  background: #e5e7eb; /* gray-200 */
}

/* ── Input styles ─────────────────────────── */
.input-field {
  width: 100%;
  background: #ffffff;
  border: 1px solid #cbd5e1; /* slate-300 */
  border-radius: 8px;
  padding: 0.625rem 1rem;
  color: #0f172a; /* slate-900 */
  font-size: 0.875rem;
  transition: all 0.2s ease;
  outline: none;
  box-shadow: 0 1px 2px 0 rgb(0 0 0 / 0.05);
}
.input-field.pl-10 {
  padding-left: 2.5rem;
}
.input-field.pl-11 {
  padding-left: 2.75rem;
}
.input-field:focus {
  border-color: #3b82f6; /* blue-500 */
  box-shadow: 0 0 0 3px rgb(59 130 246 / 0.2);
}
.input-field::placeholder {
  color: #94a3b8; /* slate-400 */
}

/* ── Badge ────────────────────────────────── */
.badge {
  display: inline-flex;
  align-items: center;
  gap: 0.25rem;
  padding: 0.25rem 0.6rem;
  border-radius: 4px;
  font-size: 0.75rem;
  font-weight: 600;
  letter-spacing: 0.025em;
}
.badge-high    { background: #fee2e2; color: #dc2626; }
.badge-medium  { background: #fef9c3; color: #ca8a04; }
.badge-low     { background: #dcfce7; color: #16a34a; }
.badge-pending { background: #f3f4f6; color: #4b5563; }
.badge-resolved { background: #dcfce7; color: #16a34a; }
```

**`app/api/analyze/route.ts`**
```typescript
import { ai } from "@/lib/gemini";

export async function POST(req: Request) {
  try {
    const { description, location } = await req.json();

    console.log("Location:", location);

    const response = await ai.models.generateContent({
      model: "gemini-3.1-flash-lite",
      contents: \`
You are a civic issue analysis agent.

Analyze the following issue report:

\${description}

Respond ONLY with a valid JSON object.

{
  "category": "Road Hazard",
  "severity": "High",
  "summary": "Large pothole near a school creating danger for pedestrians and vehicles.",
  "suggestedAction": "Immediate road inspection and repair recommended."
}
\`,
    });

    const text = response.text?.trim() || "";

    console.log("Gemini Response:");
    console.log(text);

    const cleanedText = text
      .replace(/\`\`\`json/g, "")
      .replace(/\`\`\`/g, "")
      .trim();

    const parsed = JSON.parse(cleanedText);

    return Response.json(parsed);
  } catch (error) {
    console.error("Analyze API Error:", error);

    return Response.json(
      {
        category: "Unknown",
        severity: "Medium",
        summary:
          "The AI service encountered an error while processing this report.",
        suggestedAction:
          "Please try again or provide a more detailed description.",
      },
      { status: 200 },
    );
  }
}
```

**`services/reportService.ts`**
```typescript
import { db } from "@/lib/firebase";
import { addDoc, collection, serverTimestamp } from "firebase/firestore";

export const saveReport = async (report: any) => {
  await addDoc(collection(db, "reports"), {
    ...report,
    reporterName: report.reporterName || "Anonymous",
    reporterUid: report.reporterUid || null,
    status: report.status || "Pending",
    confirmations: report.confirmations || 0,
    createdAt: serverTimestamp(),
  });
};
```
